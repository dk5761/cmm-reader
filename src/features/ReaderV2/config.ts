/**
 * Reader configuration
 * Extracted magic numbers from useReaderStoreV2.ts
 */
export const READER_CONFIG = {
  /** Delay after seeking before resuming scroll tracking (ms) */
  SEEK_DEBOUNCE_MS: 100,
} as const;
